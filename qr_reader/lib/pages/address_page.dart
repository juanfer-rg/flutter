import 'package:flutter/material.dart';

class AddressPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Direcciones Page'),
    );
  }
}
